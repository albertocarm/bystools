% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/km2_install_all.R
\name{km2_install_all}
\alias{km2_install_all}
\title{One-shot, fault-tolerant installation of every dependency}
\usage{
km2_install_all(
  r_packages = TRUE,
  python = TRUE,
  tesseract = TRUE,
  tesseract_silent = TRUE,
  envname = "r-bystools"
)
}
\arguments{
\item{r_packages}{Logical; install the R dependencies. Default \code{TRUE}.}

\item{python}{Logical; set up the Python environment. Default \code{TRUE}.}

\item{tesseract}{Logical; install the Tesseract OCR engine. Default \code{TRUE}.}

\item{tesseract_silent}{Logical; unattended Tesseract install (no GUI). Default \code{TRUE}.}

\item{envname}{Python environment name. Default \code{"r-bystools"}.}
}
\value{
(Invisibly) a named character vector: the status of each step.
}
\description{
Installs \strong{everything} \code{bystools} needs in a single run: the R package
dependencies, the Python environment for the automatic digitizer, and the
Tesseract OCR engine. Each step is isolated and tried with several fallback
methods, so a failure in one component does not abort the rest. A summary is
printed at the end listing what succeeded and what (if anything) still needs
attention.

R packages are installed with this cascade: skip if already present →
\code{install.packages()} (CRAN) → \code{remotes::install_github()} (for the two GitHub
packages) → \code{pak::pkg_install()} as a last resort.

Python is set up via \code{\link[=km2_setup_python]{km2_setup_python()}} (virtualenv → standalone Python →
conda fallback) and Tesseract via \code{\link[=km2_install_tesseract]{km2_install_tesseract()}}.
}
\examples{
\dontrun{
  bystools::km2_install_all()   # run once, then restart R
}

}
\seealso{
\code{\link[=km2_setup_python]{km2_setup_python()}}, \code{\link[=km2_install_tesseract]{km2_install_tesseract()}}, \code{\link[=km2bayes]{km2bayes()}}
}
