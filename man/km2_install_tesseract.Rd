% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/km2_install_tesseract.R
\name{km2_install_tesseract}
\alias{km2_install_tesseract}
\title{Download and install the Tesseract OCR engine (Windows helper)}
\usage{
km2_install_tesseract(
  silent = FALSE,
  mirror = "https://digi.bib.uni-mannheim.de/tesseract/"
)
}
\arguments{
\item{silent}{Logical; attempt an unattended install (Windows only). Default \code{FALSE}.}

\item{mirror}{Base URL of the installer directory. Default is the UB-Mannheim mirror.}
}
\value{
(Invisibly) the path to the downloaded installer (Windows), or \code{NULL}.
}
\description{
Installs the Tesseract OCR engine required by the automatic image pre-processing
of \code{\link[=km2bayes]{km2bayes()}}. On Windows it fetches the latest 64-bit installer from the
UB-Mannheim mirror and runs it.

On macOS it uses Homebrew when available; on Linux it prints the command for the
detected package manager.
}
\details{
Two modes on Windows:
\itemize{
\item \code{silent = FALSE} (default): launches the official GUI installer; the user
clicks Next / Install / Finish with the default folder. The app then
auto-detects \verb{C:\\\\Program Files\\\\Tesseract-OCR}.
\item \code{silent = TRUE}: performs an unattended install into a user-writable
folder (\verb{\%LOCALAPPDATA\%\\\\Programs\\\\Tesseract-OCR}, no admin needed). A single
UAC prompt may still appear depending on the installer build.
}
The bundled \code{kmdig.py} auto-detects both locations, so no \code{PATH} editing is needed.
}
\seealso{
\code{\link[=km2_setup_python]{km2_setup_python()}}, \code{\link[=km2bayes]{km2bayes()}}
}
